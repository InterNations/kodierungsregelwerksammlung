language: php

sudo: false

php:
  - 7.1
  - 7.2
  - 7.3
  - nightly
  - hhvm

matrix:
  allow_failures:
    - php: hhvm

before_script:
  - composer install

script:
  - ./vendor/bin/phpcs --standard=ruleset.xml ./src/InterNations/Sniffs
  - ./vendor/bin/phpunit
